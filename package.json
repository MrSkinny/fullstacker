{
  "name": "fullstacker",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "body-parser": "^1.17.1",
    "concurrently": "^3.4.0",
    "express": "^4.15.2",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-redux": "^5.0.3",
    "redux": "^3.6.0",
    "redux-actions": "^2.0.1",
    "redux-thunk": "^2.2.0"
  },
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "start": "node index-compiled.js",
    "client": "react-scripts start",
    "client:copy": "mkdir -p fsbuild/public && cp -r build/* fsbuild/public",
    "build": "react-scripts build",
    "build:all": "npm run clean && npm run build && npm run server:build && npm run client:copy",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "server": "nodemon --watch server --exec babel-node server/index.js",
    "server:build": "mkdir -p fsbuild && babel server --out-dir fsbuild",
    "clean": "rm -rf fsbuild"
  },
  "proxy": "http://localhost:3001"
}
